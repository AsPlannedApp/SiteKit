{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "footer content",
  "type": "object",
  "required": ["name", "role"],
  "properties": {
    "name": { "type": "string" },
    "role": { "type": "string" },
    "badges": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["image", "alt"],
        "properties": {
          "image": { "type": "string", "description": "module-relative path, resolved via ctx.asset()" },
          "alt": { "type": "string" },
          "href": { "type": "string" }
        }
      }
    },
    "extraHtml": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Raw HTML strings inserted as-is after badges -- for content that can't go through ctx.asset() (e.g. externally-hosted W3C validator badges, a copyright line, an email link). Author-controlled; NOT escaped. Checked for tag-balance by `npm run check` (see src/core/html-lint.js), but not otherwise sanitized."
    }
  }
}
